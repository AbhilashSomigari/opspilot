# Previous incident: catalog 503 propagated to checkout

Catalog emitted 503 responses. Checkout converted the dependency HTTP failure into 502 responses. Comparing service-level error rates and trace child spans identified catalog as the origin. A catalog rollback resolved the incident.
